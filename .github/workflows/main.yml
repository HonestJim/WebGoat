on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: No-op
        run: echo "sigma_analysis disabled - private DockerHub image requires admin-configured secrets"

