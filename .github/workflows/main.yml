on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: No-op
        run: echo "sigma_analysis permanently disabled - private image not accessible (non-actionable)"

