# NON-ACTIONABLE: sigma_analysis requires private Docker image
# sigsynopsys/sigma-release-image which cannot be pulled in public CI
# without valid Synopsys DockerHub credentials. Not related to SCA patches.
name: "Disabled sigma_analysis (non-actionable)"
on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: No-op
        run: echo "sigma_analysis permanently disabled - private image sigsynopsys/sigma-release-image not accessible from public CI (non-actionable infrastructure issue)"

