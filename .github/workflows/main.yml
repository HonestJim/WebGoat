on: [workflow_dispatch]

jobs:
  noop:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - name: No-op
        run: echo "sigma_analysis disabled - private image sigsynopsys/sigma-release-image not accessible from this branch context"

